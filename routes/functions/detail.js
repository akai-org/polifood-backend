const { error, success } = require('./')
const db = require('../../models')

module.exports.findDetail = (req, res) => {
    error.notImplemented(req, res)
}

module.exports.updateDetail = (req, res) => {
    error.notImplemented(req, res)
}